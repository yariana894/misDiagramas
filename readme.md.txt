# Instrucciones para subir los cambios a github

##La primera vez que lo hacemos:

1- git init

//Creamos el fichero

- 2- git add
- 3-git commit -m "mensajito que queremos que nos ponga en github"
- 4-git remote add origin https://github.com/**xxxx**/misdiagramas.git
- 5-git push origin master

a partie de la segunda vez

creamos el fichero
git add .
git commit -m"mensaje"
git push origin master